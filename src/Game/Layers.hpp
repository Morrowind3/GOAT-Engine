#ifndef GOAT_ENGINE_LAYERS_HPP
#define GOAT_ENGINE_LAYERS_HPP

enum LAYER {
    // Layers < 100 have parallax effect, the lower, the more parralax
    UI = 200,
    PARALLAX_BACKGROUND = 90,
    CHARACTER = 150,
    TILES_FRONT = 160,
    TILES_BACK = 140
};

#endif //GOAT_ENGINE_LAYERS_HPP
